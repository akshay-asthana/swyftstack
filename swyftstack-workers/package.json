{
  "name": "swyftstack-workers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "prisma generate --schema=../swyftstack-shared/prisma/schema.prisma",
    "dev": "tsx watch src/index.ts",
    "prestart": "prisma generate --schema=../swyftstack-shared/prisma/schema.prisma",
    "start": "tsx src/index.ts",
    "scheduler": "tsx src/scheduler.ts",
    "prebuild": "prisma generate --schema=../swyftstack-shared/prisma/schema.prisma",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "swyftstack-shared": "*"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
