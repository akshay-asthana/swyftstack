// Merged into the Infrastructure hub (§5). Kept as a redirect so old links work.
import { redirect } from "next/navigation";

export default function InfraOverviewRedirect() {
  redirect("/infrastructure");
}
